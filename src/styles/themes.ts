export interface ThemeParams {
    colors: {
        background: string
        text: string
        primary: string
    }
}

const theme: ThemeParams = {
    colors: {
        background: '#f7f7f9',
        text: '#334',
        primary: '#0099ff',
    },
}

export default theme
